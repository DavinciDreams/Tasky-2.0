# Tasky 2.0 Environment Variables
# Copy this file to .env and adjust values as needed.

# --- Database ---

# Path to the SQLite database file
# Default: ./data/tasky.db
TASKY_DB_PATH=./data/tasky.db

# SQLite journal mode (DELETE or WAL)
# WAL enables Write-Ahead Logging for better concurrent read performance
# Default: DELETE
TASKY_SQLITE_JOURNAL=DELETE

# --- Application ---

# Environment type (development | production)
# Controls DevTools, CSP policy, logging, and sound behavior
# Default: production
NODE_ENV=production

# Override to force-enable DevTools in the assistant window
# Set to 1 to enable, leave unset to disable
# TASKY_DEVTOOLS=1

# Logging verbosity (debug | info | warn | error)
# Falls back to NODE_ENV if not set, then defaults to info
# TASKY_LOG_LEVEL=info

# --- AI Providers ---
# Provider settings are managed via the in-app Settings UI and persisted
# in electron-store. The env vars below serve as fallbacks for the
# Vercel AI SDK and are useful for CLI/MCP agent usage.

# Google AI (Gemini) API key
# Obtain from: https://aistudio.google.com/apikey
# Supported models: gemini-2.5-pro, gemini-2.5-flash, gemini-2.5-flash-lite,
#   gemini-2.0-flash, gemini-1.5-flash
# GOOGLE_GENERATIVE_AI_API_KEY=

# LM Studio (local models via OpenAI-compatible API)
# Base URL for your local LM Studio server
# LMSTUDIO_BASE_URL=http://localhost:1234/v1
# LMSTUDIO_MODEL=
